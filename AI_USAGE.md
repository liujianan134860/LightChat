# AI_USAGE

本文记录 LightChat 开发过程中 AI 给出的方案，以及最终是否采纳和原因。记录重点不是“AI 写了什么”，而是方案经过项目上下文校验后如何落地。

## 1. 采纳：WebSocket 事件串行化处理

**背景**：IM 系统中网络事件可能并发到达，例如新消息、已读、撤回、资料更新同时触发。如果多个协程同时写 SQLite，容易出现消息顺序错乱、会话最后消息被旧事件覆盖、`lastUserSeq` 提前推进导致丢事件。

**AI 方案**：使用 Coroutines + Channel/串行处理器，让 WebSocket 收到的业务事件进入单一事件处理队列，由 `EventProcessor` 顺序写库。

**采纳原因**：

- 与 IM “先入队、再串行落库”的常见做法一致。
- 改动范围集中在客户端同步层，不需要重写协议。
- 能与 `userSeq` 游标配合，做到落库成功后再推进同步进度。

**结果**：同步事件写库顺序稳定，消息展示顺序和会话最后消息更新更可靠。

## 2. 采纳：clientSeq / conversationSeq / userSeq 三套序列号

**背景**：单靠本地时间戳无法解决离线发送、失败重发、群聊多端并发、历史分页、已读定位等问题。

**AI 方案**：

- `clientSeq`：客户端本地自增，管理发送中、失败、重发和本地临时排序。
- `conversationSeq`：服务端按会话递增，作为最终展示顺序、历史分页 cursor 和已读进度。
- `userSeq`：服务端按用户 Inbox 递增，作为增量同步进度。

**采纳原因**：

- 三者职责清晰，不互相替代。
- 能解释和支撑面试中“消息有序、断网恢复、已读同步”的关键链路。
- 与数据库设计中的 `message.client_seq`、`message.conversation_seq`、`sync_state.last_user_seq`、`inbox_events.user_seq` 对齐。

**结果**：失败消息、本地消息、服务端 ACK 消息和同步消息可以统一排序；1 万条消息中间定位也能用 `conversationSeq` 做稳定锚点。

## 3. 采纳：阿里云 OSS 私有 Bucket + 签名 URL + 本地缓存

**背景**：图片不适合直接放在 WebSocket 消息体中。项目需要支持原图、缩略图、私有访问、URL 过期刷新和本地缓存。

**AI 方案**：图片走 HTTP multipart 上传到阿里云 OSS，消息 `extra` 中保存对象 key、原图 URL、缩略图 URL、尺寸等元信息；客户端优先加载本地缓存，远端签名 URL 过期时调用服务端刷新。

**采纳原因**：

- WebSocket 只传消息元数据，避免大包阻塞消息通道。
- 私有 Bucket 更接近真实生产环境。
- 本地缓存可以显著减少头像和图片重复下载。

**结果**：图片发送、缩略图展示、全屏拉取原图、合并转发图片展示和下载相册链路都统一到缓存管理器中。

## 4. 未采纳：接入厂商推送 SDK 完成真实离线推送

**背景**：用户希望应用被杀掉、WebSocket 断开后仍能收到通知，但当前只有三台测试设备，暂时不需要拔掉 USB 仍能推送。

**AI 方案**：生产方案应接入 FCM/小米/华为/OPPO/vivo 等厂商推送 SDK，由客户端上报 push token，服务端调用厂商 API。

**未采纳原因**：

- 厂商 SDK 接入成本高，账号、证书、包名、签名、审核都需要额外配置。
- 当前阶段目标是本地联调，不要求脱离 USB 和 ADB。
- 引入多个厂商 SDK 会增加项目体积和复杂度。

**替代方案**：采纳本地 Mock Push。服务端发现目标用户 WebSocket 不在线时，按用户 ID 找到 ADB 设备，发送显式广播唤起 `BroadcastReceiver` 展示通知。

## 5. 采纳：聊天记录分页用 conversationSeq 而不是 createTime

**背景**：性能测试中向一个会话插入 1 万条消息，每 100 条共用同一个时间戳。用 `createTime` 做分页 cursor 时，上滑可能出现触顶、断层、重复加载或跳过消息。

**AI 方案**：历史分页使用服务端分配的 `conversationSeq` 作为 cursor，搜索定位时加载目标消息前后窗口，并保持滚动锚点。

**采纳原因**：

- `conversationSeq` 在会话内严格递增，比时间戳更适合做分页边界。
- 能同时支撑搜索定位、向上加载历史、向下加载新消息。

**结果**：1 万条消息中间定位后，可以继续向上和向下分页；搜索结果定位到屏幕中部并高亮。

## 6. 采纳：图片 Bitmap 解码的 inSampleSize 必须是 2 的幂

**背景**：部分设备上缩略图生成失败，最终 fallback 使用原图作为缩略图，导致缩略图和原图一样大。

**AI 方案**：按 Android 规范计算 `BitmapFactory.Options.inSampleSize`，只使用 1、2、4、8、16 等 2 的幂。

**采纳原因**：

- 这是 Android Bitmap 解码的明确约束。
- 修复点集中，可通过单元/构建测试验证。

**结果**：`decodeSampledBitmap`、`compressImage`、相册缩略图和预览缩略图统一使用合法采样值，避免部分真机 `decodeFile` 返回 null。

## 7. 采纳后调整：Bootstrap 全量同步而不是截断 200 条

**背景**：早期为了加快登录，AI 建议 Bootstrap 只同步最近 200 条消息，其余依赖后续历史分页。但当前真机性能验证需要新账号首次登录后完整看到 200 个会话和 1 万条测试消息。

**最终决策**：改回 Bootstrap 全量同步，并简化服务端日志为“全量数据同步完成”。

**采纳/调整原因**：

- Demo 和性能验收阶段，完整数据可见性优先于首屏极致速度。
- 后续如果要生产化，再拆成 Bootstrap 基础数据 + History API 分页拉取更合理。

**结果**：新账号登录后能同步完整会话、联系人、消息、置顶和免打扰状态。

## 8. 采纳：头像和图片统一本地缓存优先

**背景**：抓包发现头像可能以 URL/Base64 混用；多处页面只显示名字首字母头像；断网后本地头像缓存被清理导致无法显示。

**AI 方案**：抽象统一头像加载与图片缓存策略：内存 LRU → 磁盘缓存 → 本地路径 → 远端下载 → 默认占位；URL 刷新后同时更新本地数据库和服务端数据。

**采纳原因**：

- 头像出现场景很多，必须避免每个页面各写一套逻辑。
- 本地缓存优先能提升列表滑动性能，也能支持断网查看。

**结果**：会话列表、联系人、好友申请、搜索、转发列表、名片推荐、群成员、合并转发详情等页面统一加载真实头像。
