# LightChat 双端数据库表设计说明

本文档完整描述 LightChat 客户端 SQLite 和服务端 MySQL 的所有表结构、索引、迁移历史和设计决策。

---

## 一、客户端 SQLite

**数据库文件**：`lightchat.db`
**当前版本**：`DATABASE_VERSION = 12`
**基类**：`SQLiteOpenHelper`（非 Room）

### 设计原则

- **多账号隔离**：业务缓存表以 `owner_user_id` 作为主键组成部分，支持同一设备切换账号不串数据
- **软删除**：消息和会话使用 `is_deleted` 标志，不物理删除
- **冲突策略**：通用 INSERT 使用 `CONFLICT_REPLACE`（upsert），好友关系使用 `CONFLICT_IGNORE`（防重复）
- **反范式冗余**：会话表冗余 `last_message_*` 字段避免每次列表渲染时 JOIN 消息表

---

### 1. user — 用户资料缓存表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| user_id | TEXT | PRIMARY KEY | 用户 ID |
| nickname | TEXT | | 昵称 |
| avatar | TEXT | | 头像旧字段/兼容字段（URL / Base64 / 空字符串） |
| avatar_url | TEXT | DEFAULT '' | 头像云端 URL |
| avatar_version | INTEGER | DEFAULT 0 | 头像版本号，用于缓存刷新 |
| signature | TEXT | | 个性签名 |
| region | TEXT | | 地区 |

```sql
CREATE TABLE IF NOT EXISTS user (
    owner_user_id TEXT NOT NULL,
    user_id TEXT NOT NULL,
    nickname TEXT,
    avatar TEXT,
    avatar_url TEXT DEFAULT '',
    avatar_version INTEGER DEFAULT 0,
    signature TEXT,
    region TEXT,
    PRIMARY KEY(owner_user_id, user_id)
)
```

---

### 2. friend — 好友关系缓存表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| user_id | TEXT | PRIMARY KEY | 当前用户 ID |
| friend_id | TEXT | PRIMARY KEY | 好友用户 ID |

```sql
CREATE TABLE IF NOT EXISTS friend (
    owner_user_id TEXT NOT NULL,
    user_id TEXT,
    friend_id TEXT,
    PRIMARY KEY(owner_user_id, user_id, friend_id)
)
```

---

### 3. friend_request — 好友申请缓存表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| request_id | TEXT | PRIMARY KEY | 申请 ID |
| from_user_id | TEXT | NOT NULL | 申请方用户 ID |
| to_user_id | TEXT | NOT NULL | 接收方用户 ID |
| from_nickname | TEXT | | 申请方昵称（快照） |
| message | TEXT | | 验证消息 |
| status | INTEGER | DEFAULT 0 | 0=PENDING, 1=ACCEPTED, 2=REJECTED |
| create_time | INTEGER | NOT NULL | 创建时间（毫秒） |

```sql
CREATE TABLE IF NOT EXISTS friend_request (
    owner_user_id TEXT NOT NULL,
    request_id TEXT NOT NULL,
    from_user_id TEXT NOT NULL,
    to_user_id TEXT NOT NULL,
    from_nickname TEXT,
    message TEXT,
    status INTEGER DEFAULT 0,
    create_time INTEGER NOT NULL,
    PRIMARY KEY(owner_user_id, request_id)
)
```

---

### 4. conversation — 本地会话列表缓存表

承担会话列表快速渲染的核心职责。`owner_user_id` 实现多账号隔离。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| conversation_id | TEXT | PRIMARY KEY | 会话 ID |
| type | INTEGER | | 0=SINGLE, 1=GROUP, 2=SYSTEM |
| target_id | TEXT | | 单聊对方 ID 或群 ID |
| title | TEXT | | 显示标题 |
| avatar | TEXT | | 会话头像 |
| avatar_url | TEXT | DEFAULT '' | 会话头像云端 URL |
| avatar_version | INTEGER | DEFAULT 0 | 会话头像版本 |
| last_message_id | TEXT | | 最后一条消息 ID |
| last_message_content | TEXT | | 最后一条消息摘要 |
| last_message_time | INTEGER | | 最后一条消息时间 |
| last_thumbnail | TEXT | DEFAULT '' | 最后消息缩略图（v7 新增） |
| unread_count | INTEGER | DEFAULT 0 | 未读计数 |
| at_me | INTEGER | DEFAULT 0 | 是否有人 @ 我 |
| at_me_count | INTEGER | DEFAULT 0 | @ 我次数（v8 新增） |
| is_pinned | INTEGER | DEFAULT 0 | 是否置顶 |
| pinned_time | INTEGER | DEFAULT 0 | 置顶时间 |
| is_hidden | INTEGER | DEFAULT 0 | 是否隐藏 |
| is_deleted | INTEGER | DEFAULT 0 | 是否删除 |
| mute | INTEGER | DEFAULT 0 | 是否免打扰 |
| manual_unread | INTEGER | DEFAULT 0 | 是否手动标记未读 |

```sql
CREATE TABLE IF NOT EXISTS conversation (
    owner_user_id TEXT NOT NULL,
    conversation_id TEXT NOT NULL,
    type INTEGER,
    target_id TEXT,
    title TEXT,
    avatar TEXT,
    avatar_url TEXT DEFAULT '',
    avatar_version INTEGER DEFAULT 0,
    last_message_id TEXT,
    last_message_content TEXT,
    last_message_time INTEGER,
    last_thumbnail TEXT DEFAULT '',
    unread_count INTEGER DEFAULT 0,
    at_me INTEGER DEFAULT 0,
    at_me_count INTEGER DEFAULT 0,
    is_pinned INTEGER DEFAULT 0,
    pinned_time INTEGER DEFAULT 0,
    is_hidden INTEGER DEFAULT 0,
    is_deleted INTEGER DEFAULT 0,
    mute INTEGER DEFAULT 0,
    manual_unread INTEGER DEFAULT 0,
    PRIMARY KEY(owner_user_id, conversation_id)
)
```

**排序索引**（置顶优先）：
```sql
CREATE INDEX idx_conversation_order ON conversation(
    owner_user_id, is_pinned DESC, pinned_time DESC, last_message_time DESC
)
```

---

### 5. conversation_member — 会话成员状态缓存表

参考 Rocket.Chat `subscription` / Signal `thread` 设计，存储会话级别的每用户个性化状态。与 `conversation` 表形成补充——`conversation` 存会话基础信息 + 最后消息快照，`conversation_member` 存每用户个性化设置。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| conversation_id | TEXT | PRIMARY KEY | 会话 ID |
| user_id | TEXT | PRIMARY KEY | 成员用户 ID |
| display_name_cache | TEXT | | 成员显示名缓存 |
| role | INTEGER | DEFAULT 1 | 0=OWNER, 1=MEMBER |
| last_read_seq | INTEGER | DEFAULT 0 | 该成员最后已读的 conversationSeq |
| last_delivered_seq | INTEGER | DEFAULT 0 | 该成员最后送达的 conversationSeq（预留） |
| unread_count | INTEGER | DEFAULT 0 | 该成员视角未读数 |
| mention_count | INTEGER | DEFAULT 0 | 该成员视角 @ 计数 |
| is_pinned | INTEGER | DEFAULT 0 | 该成员是否置顶此会话 |
| pinned_time | INTEGER | DEFAULT 0 | 置顶时间 |
| mute_until | INTEGER | DEFAULT 0 | 免打扰截止时间，0=未设置 |
| is_hidden | INTEGER | DEFAULT 0 | 该成员是否隐藏此会话 |
| last_seen_time | INTEGER | DEFAULT 0 | 最后进入/读到会话时间 |
| joined_at | INTEGER | DEFAULT 0 | 加入会话时间 |

```sql
CREATE TABLE IF NOT EXISTS conversation_member (
    owner_user_id TEXT NOT NULL,
    conversation_id TEXT NOT NULL,
    user_id TEXT NOT NULL,
    display_name_cache TEXT,
    role INTEGER DEFAULT 1,
    last_read_seq INTEGER DEFAULT 0,
    last_delivered_seq INTEGER DEFAULT 0,
    unread_count INTEGER DEFAULT 0,
    mention_count INTEGER DEFAULT 0,
    is_pinned INTEGER DEFAULT 0,
    pinned_time INTEGER DEFAULT 0,
    mute_until INTEGER DEFAULT 0,
    is_hidden INTEGER DEFAULT 0,
    last_seen_time INTEGER DEFAULT 0,
    joined_at INTEGER DEFAULT 0,
    PRIMARY KEY(owner_user_id, conversation_id, user_id)
)
```

**排序索引**：
```sql
CREATE INDEX idx_conversation_member_owner_order ON conversation_member(
    owner_user_id, is_pinned DESC, pinned_time DESC, last_seen_time DESC
)
```

当前使用情况：`GroupDao.upsertConversationMemberRead()` 和 `getConversationReadSeqs()` 已使用此表，用于群聊已读统计。后续计划将 `conversation` 中的用户态字段（置顶/免打扰/隐藏/未读）迁移至此表。

---

### 6. message — 本地消息缓存表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| message_id | TEXT | PRIMARY KEY | 消息全局 ID |
| conversation_id | TEXT | NOT NULL | 所属会话 ID |
| sender_id | TEXT | NOT NULL | 发送者用户 ID |
| receiver_id | TEXT | | 单聊接收者 ID |
| group_id | TEXT | | 群 ID |
| message_type | INTEGER | | 0=TEXT, 1=IMAGE, 2=VOICE, 3=VIDEO, 4=FILE, 5=USER_CARD, 6=GROUP_CARD, 7=MERGE_FORWARD, 99=SYSTEM |
| content | TEXT | | 文本内容或摘要 |
| status | INTEGER | | 0=CREATED, 1=SENDING, 2=SENT, 3=DELIVERED, 4=READ, 5=FAILED |
| client_seq | INTEGER | | 客户端本地自增序号 |
| conversation_seq | INTEGER | | 服务端分配的会话内序号 |
| user_seq | INTEGER | | 服务端分配的用户维度序号 |
| send_time | INTEGER | | 发送时间（毫秒） |
| create_time | INTEGER | | 本地创建时间（毫秒） |
| quote_message_id | TEXT | | 引用回复的消息 ID（预留） |
| original_message_id | TEXT | | 原始消息 ID（转发/撤回溯源） |
| is_deleted | INTEGER | DEFAULT 0 | 软删除标志 |
| is_recalled | INTEGER | DEFAULT 0 | 已撤回标志 |
| extra | TEXT | | JSON 扩展字段（@提及数组、名片数据、合并转发包等） |

```sql
CREATE TABLE IF NOT EXISTS message (
    owner_user_id TEXT NOT NULL,
    message_id TEXT NOT NULL,
    conversation_id TEXT NOT NULL,
    sender_id TEXT NOT NULL,
    receiver_id TEXT,
    group_id TEXT,
    message_type INTEGER,
    content TEXT,
    status INTEGER,
    client_seq INTEGER,
    conversation_seq INTEGER,
    user_seq INTEGER,
    send_time INTEGER,
    create_time INTEGER,
    quote_message_id TEXT,
    original_message_id TEXT,
    is_deleted INTEGER DEFAULT 0,
    is_recalled INTEGER DEFAULT 0,
    extra TEXT,
    PRIMARY KEY(owner_user_id, message_id)
)
```

**查询索引**（分页翻页核心）：
```sql
CREATE INDEX idx_message_conversation_seq ON message(
    owner_user_id, conversation_id, conversation_seq DESC
)
CREATE INDEX idx_message_conversation_time ON message(
    owner_user_id, conversation_id, create_time DESC
)
```

**三套序列号说明**：
| 序列号 | 赋值方 | 用途 |
|--------|--------|------|
| `client_seq` | 客户端 `LightChatApplication.nextClientSeq` 自增 | 本地消息排序（离线消息无 conversationSeq 时） |
| `conversation_seq` | 服务端 `EventService.nextConversationSeq()` | 会话内严格递增排序、翻页锚点、已读标记 |
| `user_seq` | 服务端 `EventService.nextUserSeq()` | 用户维度事件同步游标、增量拉取 |

**extra 字段用途**：
- `@提及`：`{"atUserIds":["u1002","u1003"]}`
- `用户名片`：`{"userId":"u1002","nickname":"张三","avatar":"..."}`
- `群名片`：`{"groupId":"g2001","groupName":"技术交流群"}`
- `合并转发`：`{"title":"...","messages":[...]}`
- `消息撤回`：`{"recalledContent":"原始消息文本"}`

---

### 7. message_receipt — 消息回执缓存表

为群聊已读回执设计的结构化存储。每条回执记录某用户对某消息的已读/送达状态。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| conversation_id | TEXT | NOT NULL | 会话 ID |
| message_id | TEXT | PRIMARY KEY | 消息 ID |
| user_id | TEXT | PRIMARY KEY | 回执用户 |
| receipt_type | INTEGER | PRIMARY KEY | 1=DELIVERED, 2=READ |
| conversation_seq | INTEGER | DEFAULT 0 | 被回执消息的 conversationSeq |
| receipt_time | INTEGER | DEFAULT 0 | 回执时间 |

```sql
CREATE TABLE IF NOT EXISTS message_receipt (
    owner_user_id TEXT NOT NULL,
    conversation_id TEXT NOT NULL,
    message_id TEXT NOT NULL,
    user_id TEXT NOT NULL,
    receipt_type INTEGER NOT NULL,
    conversation_seq INTEGER DEFAULT 0,
    receipt_time INTEGER DEFAULT 0,
    PRIMARY KEY(owner_user_id, message_id, user_id, receipt_type)
)
```

**索引**：
```sql
CREATE INDEX idx_message_receipt_message ON message_receipt(
    owner_user_id, conversation_id, message_id
)
```

当前使用情况：表已建立但客户端业务尚未写入，群聊已读目前由 `ConversationReadSeqs` (conversation_member) 统计。

---

### 8. im_group — 群资料缓存表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| group_id | TEXT | PRIMARY KEY | 群 ID |
| group_name | TEXT | | 群名称 |
| avatar | TEXT | | 群头像 |
| avatar_url | TEXT | DEFAULT '' | 群头像云端 URL |
| avatar_version | INTEGER | DEFAULT 0 | 群头像版本 |
| owner_id | TEXT | | 群主用户 ID |
| member_count | INTEGER | | 成员数量 |
| create_time | INTEGER | | 创建时间 |

```sql
CREATE TABLE IF NOT EXISTS im_group (
    owner_user_id TEXT NOT NULL,
    group_id TEXT NOT NULL,
    group_name TEXT,
    avatar TEXT,
    avatar_url TEXT DEFAULT '',
    avatar_version INTEGER DEFAULT 0,
    owner_id TEXT,
    member_count INTEGER,
    create_time INTEGER,
    PRIMARY KEY(owner_user_id, group_id)
)
```

---

### 9. group_member — 群成员缓存表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| group_id | TEXT | PRIMARY KEY | 群 ID |
| user_id | TEXT | PRIMARY KEY | 成员用户 ID |
| nickname | TEXT | | 成员昵称（快照） |
| avatar | TEXT | | 成员头像（快照） |
| avatar_url | TEXT | DEFAULT '' | 成员头像云端 URL |
| avatar_version | INTEGER | DEFAULT 0 | 成员头像版本 |
| role | INTEGER | | 0=OWNER, 1=ADMIN, 2=MEMBER |
| alias_in_group | TEXT | | 群内备注名 |
| join_time | INTEGER | | 入群时间 |

```sql
CREATE TABLE IF NOT EXISTS group_member (
    owner_user_id TEXT NOT NULL,
    group_id TEXT,
    user_id TEXT,
    nickname TEXT,
    avatar TEXT,
    avatar_url TEXT DEFAULT '',
    avatar_version INTEGER DEFAULT 0,
    role INTEGER,
    alias_in_group TEXT,
    join_time INTEGER,
    PRIMARY KEY(owner_user_id, group_id, user_id)
)
```

---

### 10. sync_state — 同步游标表

存储增量同步的游标位置。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | TEXT | PRIMARY KEY | 缓存所属账号 |
| key | TEXT | PRIMARY KEY | 游标名，目前仅使用 `last_user_seq` |
| value | INTEGER | NOT NULL | 游标值 |

```sql
CREATE TABLE IF NOT EXISTS sync_state (
    owner_user_id TEXT NOT NULL,
    key TEXT NOT NULL,
    value INTEGER NOT NULL,
    PRIMARY KEY(owner_user_id, key)
)
```

---

### 11. auth_session — 本地登录会话缓存表

记录设备上保存过的 JWT 登录状态，用于启动时快速判断是否需要重新登录。实际鉴权由服务端 JWT 签名和 `auth_sessions` 表决定。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| token_id | TEXT | PRIMARY KEY | JWT `jti` |
| user_id | TEXT | NOT NULL | 用户 ID |
| token | TEXT | NOT NULL | JWT 原文 |
| issued_at | INTEGER | DEFAULT 0 | 签发时间（秒） |
| expires_at | INTEGER | DEFAULT 0 | 过期时间（秒） |
| login_time | INTEGER | DEFAULT 0 | 本地登录时间（毫秒） |
| last_used_at | INTEGER | DEFAULT 0 | 最近使用时间 |
| is_active | INTEGER | DEFAULT 1 | 1=当前有效, 0=已退出或被替换 |

```sql
CREATE TABLE IF NOT EXISTS auth_session (
    token_id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    token TEXT NOT NULL,
    issued_at INTEGER DEFAULT 0,
    expires_at INTEGER DEFAULT 0,
    login_time INTEGER DEFAULT 0,
    last_used_at INTEGER DEFAULT 0,
    is_active INTEGER DEFAULT 1
)
```

**索引**：
```sql
CREATE INDEX idx_auth_session_user ON auth_session(user_id, is_active, expires_at)
```

---

### 数据库版本迁移历史

| 版本 | 变更 |
|------|------|
| 1 | 初始创建：user, friend, conversation, message, im_group, group_member, sync_state |
| 2 | 新增 `friend_request` 表 |
| 3 | `user` 表新增 `region TEXT DEFAULT ''` |
| 4 | **多账号改造**：所有表（除 friend_request）新增 `owner_user_id`，通过临时表重命名→重建→复制→删除实现 |
| 5 | 新增 `conversation_member` + `message_receipt` 表及索引 |
| 6 | 新增 `auth_session` 表及索引 |
| 7 | `conversation` 表新增 `last_thumbnail TEXT DEFAULT ''` |
| 8 | `conversation` 表新增 `at_me_count INTEGER DEFAULT 0` |
| 9 | `friend_request` 表新增 `owner_user_id`，主键改为 `(owner_user_id, request_id)` |
| 10 | `user` 表新增 `avatar_url`、`avatar_version`，用于头像 URL 与缓存版本同步 |
| 11 | `user`、`friend` 表迁移为 `owner_user_id` 隔离；`group_member` 新增头像 URL/版本字段 |
| 12 | `conversation`、`im_group` 新增头像 URL/版本字段 |

---

## 二、服务端 MySQL

**数据库**：`lightchat`
**持久化方式**：MySQL 结构化业务表持久化，不再保存 `lightchat_state` 整体状态快照

### 架构说明

当前服务端采用 **DataStore 运行时缓存 + MySQL 结构化表持久化**：
1. 服务启动时，`MySqlStatePersistence.load()` 直接读取 `users`、`credentials`、`conversations`、`messages`、`chat_groups`、`inbox_events` 等结构化表，恢复到运行时 `DataStore`。
2. 运行期间，业务逻辑仍通过 `DataStore` 聚合读写，减少各处理器直接操作 SQL 的复杂度。
3. `DataStore` 发生变化后，异步持久化任务调用 `MySqlStatePersistence.save()`，在事务内对各结构化表执行 upsert。
4. 服务端已删除 `lightchat_state` 整体快照表，MySQL 结构化表就是服务重启恢复的数据来源。

服务端写入流程：业务处理器 → `DataStore` 变更 → debounce 触发持久化 → `MySqlStatePersistence.save()` → MySQL 结构化表 upsert。

---

### 1. users — 用户表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | VARCHAR(64) | PRIMARY KEY | 用户 ID |
| nickname | VARCHAR(255) | | 昵称 |
| avatar | MEDIUMTEXT | | 头像旧字段/兼容字段（URL/Base64） |
| avatar_url | VARCHAR(1024) | DEFAULT '' | 头像云端 URL |
| avatar_version | INT | DEFAULT 0 | 头像版本号 |
| signature | VARCHAR(1024) | | 签名 |
| region | VARCHAR(255) | | 地区 |
| created_at | BIGINT | | 创建时间 |

---

### 2. credentials — 密码凭据表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | VARCHAR(64) | PRIMARY KEY | 用户 ID |
| password | VARCHAR(255) | | 密码哈希 `sha256$salt$digest`；支持明文自动升级 |

---

### 3. auth_sessions — 服务端登录会话表

JWT 负责签名防篡改和过期，此表负责判断 `jti` 是否仍为服务端认可的有效会话。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| token_id | VARCHAR(64) | PRIMARY KEY | JWT `jti` |
| user_id | VARCHAR(64) | | 用户 ID |
| issued_at | BIGINT | | 签发时间（秒） |
| expires_at | BIGINT | | 过期时间（秒） |
| revoked_at | BIGINT | | 撤销时间，0=未撤销 |
| device_name | VARCHAR(128) | | 设备名 |
| client_ip | VARCHAR(64) | | 登录 IP |
| last_seen_at | BIGINT | | 最近鉴权时间 |

---

### 4. friendships — 好友关系表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | VARCHAR(64) | PK | 用户 ID |
| friend_id | VARCHAR(64) | PK | 好友 ID |

主键 `(user_id, friend_id)`

---

### 5. friend_requests — 好友申请表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | AUTO_INCREMENT PK | 自增主键 |
| from_user_id | VARCHAR(64) | | 申请方 |
| to_user_id | VARCHAR(64) | | 接收方 |
| from_nickname | VARCHAR(255) | | 申请方昵称快照 |
| request_message | VARCHAR(1024) | | 验证消息 |
| status | INT | | 0=PENDING, 1=ACCEPTED, 2=REJECTED |
| create_time | BIGINT | | 创建时间 |

---

### 6. conversations — 会话基础表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| conversation_id | VARCHAR(128) | PRIMARY KEY | 会话 ID |
| type | VARCHAR(32) | | SINGLE / GROUP / SYSTEM |
| group_id | VARCHAR(64) | | 关联群 ID |
| created_at | BIGINT | | 创建时间 |

---

### 7. conversation_participants — 会话参与者

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| conversation_id | VARCHAR(128) | PK | 会话 ID |
| user_id | VARCHAR(64) | PK | 参与者 ID |

主键 `(conversation_id, user_id)`。仅表达"谁在会话中"，不含状态。可被 `conversation_members` 完全覆盖。

---

### 8. conversation_members — 会话成员状态表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| conversation_id | VARCHAR(128) | PK | 会话 ID |
| user_id | VARCHAR(64) | PK | 成员 ID |
| display_name_cache | VARCHAR(255) | | 显示名缓存 |
| role | INT | | 0=OWNER, 1=MEMBER |
| last_read_seq | BIGINT | | 最后已读 conversationSeq |
| last_delivered_seq | BIGINT | | 最后送达 conversationSeq |
| unread_count | INT | | 未读计数 |
| mention_count | INT | | @ 计数 |
| is_pinned | TINYINT | | 是否置顶 |
| pinned_time | BIGINT | | 置顶时间 |
| mute_until | BIGINT | | 免打扰截止，0=未设置 |
| is_hidden | TINYINT | | 是否隐藏 |
| last_seen_time | BIGINT | | 最后访问时间 |
| joined_at | BIGINT | | 加入时间 |

主键 `(conversation_id, user_id)`。是会话列表的理想权威来源。

---

### 9. messages — 消息表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| message_id | VARCHAR(128) | PRIMARY KEY | 消息 ID |
| conversation_id | VARCHAR(128) | | 会话 ID |
| sender_id | VARCHAR(64) | | 发送者 |
| receiver_id | VARCHAR(64) | | 单聊接收者 |
| group_id | VARCHAR(64) | | 群 ID |
| message_type | INT | | 消息类型 |
| content | LONGTEXT | | 消息内容 |
| extra | LONGTEXT | | JSON 扩展 |
| client_seq | BIGINT | | 客户端序号 |
| conversation_seq | BIGINT | | 会话内序号 |
| send_time | BIGINT | | 发送时间 |
| create_time | BIGINT | | 创建时间 |

---

### 10. message_receipts — 消息回执表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| message_id | VARCHAR(128) | PK | 消息 ID |
| conversation_id | VARCHAR(128) | | 会话 ID |
| user_id | VARCHAR(64) | PK | 回执用户 |
| receipt_type | INT | PK | 1=DELIVERED, 2=READ |
| conversation_seq | BIGINT | | 被回执消息的 seq |
| event_user_seq | BIGINT | | 产生回执事件的 userSeq |
| receipt_time | BIGINT | | 回执时间 |

主键 `(message_id, user_id, receipt_type)`

---

### 11. chat_groups — 群组表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| group_id | VARCHAR(64) | PRIMARY KEY | 群 ID |
| group_name | VARCHAR(255) | | 群名称 |
| owner_id | VARCHAR(64) | | 群主用户 ID |
| created_at | BIGINT | | 创建时间 |

---

### 12. group_members — 群成员表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| group_id | VARCHAR(64) | PK | 群 ID |
| user_id | VARCHAR(64) | PK | 成员 ID |
| role | INT | | 0=OWNER, 1=MEMBER |

主键 `(group_id, user_id)`

---

### 13. user_seq_counters — 用户序列号计数器

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | VARCHAR(64) | PRIMARY KEY | 用户 ID |
| latest_user_seq | BIGINT | | 最新 userSeq |

避免每次发事件时 `MAX(inbox_events.user_seq)` 扫表。

---

### 14. conversation_seq_counters — 会话序列号计数器

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| conversation_id | VARCHAR(128) | PRIMARY KEY | 会话 ID |
| latest_conversation_seq | BIGINT | | 最新 conversationSeq |

---

### 15. inbox_events — 用户收件箱事件表

增量同步的核心表。每个事件一条记录，按 userSeq 严格递增。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| owner_user_id | VARCHAR(64) | PK | 收件用户 |
| user_seq | BIGINT | PK | 用户维度递增序号 |
| event_type | INT | | 事件类型 (1-10) |
| payload | LONGTEXT | | 事件 JSON |
| created_at | BIGINT | | 创建时间 |

主键 `(owner_user_id, user_seq)`。服务端 `SYNC` 命令即查询此表 `WHERE user_seq > lastUserSeq ORDER BY user_seq`。

---

## 三、表关系总览

```
user ────┬── friend ────── 好友关系
         ├── friend_request ─ 好友申请
         ├── group_member ── 群成员
         ├── conversation_member ─ 会话成员状态
         ├── message (sender) ─ 发送的消息
         └── auth_session ─── 登录会话
         
im_group ───┬── group_member ── 群成员
            └── conversation (GROUP) ─ 群会话

conversation ───┬── conversation_member ─ 每成员状态
                ├── message ──────────── 消息
                └── message_receipt ──── 消息回执

sync_state ─── 同步游标
inbox_events ─ 用户事件队列
```

---

## 四、关键设计决策

### 4.1 为什么 conversation 表冗余最后消息字段？

会话列表渲染是最常见的操作。如果每次从 `message` 表 `MAX(create_time)` + `ORDER BY` 查询，随着消息量增长性能会急剧下降。反范式冗余 `last_message_*` 让会话列表只需单表查询。

### 4.2 为什么需要三套序列号？

| 场景 | 依赖的序列号 |
|------|-------------|
| 客户端离线消息排序 | clientSeq（本地自增，不需要服务端） |
| 会话内消息排序和翻页锚点 | conversationSeq（严格递增，服务端分配） |
| 增量同步和事件去重 | userSeq（用户维度，服务端分配） |

三者职责不同，不可互相替代。微信 seqsvr 同样使用多套 seq。

### 4.3 为什么既有 conversation 又有 conversation_member？

这是演进中的设计：
- `conversation`：作为会话基础信息 + 当前用户的会话快照（最后消息、未读数），当前会话列表直接读取此表
- `conversation_member`：每用户每会话的状态（置顶/免打扰/已读 seq），参考成熟 IM 的 subscription 模型

后续演进方向：将 `conversation` 中的用户态字段迁至 `conversation_member`，`conversation` 只保留会话基础信息和最后消息。

### 4.4 服务端为什么删除 lightchat_state 整体快照？

早期 `lightchat_state` 可以用一份整体状态快照快速恢复内存状态，但随着结构化表逐渐完善，继续保存整体快照会带来数据冗余和排查歧义。当前服务端启动时直接从 MySQL 结构化表恢复 `DataStore`，运行时再将变化 upsert 回业务表。这样 MySQL 表既是可查询的数据来源，也是服务重启恢复来源。
