# DECISIONS

本文使用 ADR（Architecture Decision Record）格式记录 LightChat 的关键架构决策。

## ADR-001 使用 Android Compose + Kotlin/JVM 服务端

**状态**：Accepted

**背景**：项目需要同时覆盖移动端 UI、即时通讯长连接、服务端消息分发和本地数据缓存。为了降低语言切换成本，客户端和服务端都优先使用 Kotlin 技术栈。

**决策**：客户端使用 Kotlin + Jetpack Compose；服务端使用 Kotlin/JVM + Netty WebSocket + JDK HttpServer。

**后果**：

- 客户端 UI 状态和业务状态可以通过 ViewModel/StateFlow 清晰衔接。
- 服务端无需引入大型 Web 框架，便于理解 IM 协议和连接管理。
- 代价是部分能力需要手写，例如 HTTP 路由、协议处理和运行时状态管理。

## ADR-002 WebSocket 使用自定义二进制协议

**状态**：Accepted

**背景**：IM 长连接需要鉴权、心跳、消息发送、同步、撤回、已读等多类命令。如果直接使用纯文本 JSON，协议边界和命令扩展不够清晰。

**决策**：WebSocket payload 使用自定义二进制协议，头部包含 Magic、Version、Cmd、Seq、Body Length，Body 为 JSON，尾部使用 CRC32 校验。

**后果**：

- 客户端和服务端协议命令一致，便于单元测试。
- `Seq` 可用于定位请求/响应和 ACK。
- CRC32 只能检查损坏，不提供加密；生产环境仍需 HTTPS/WSS。

## ADR-003 使用 Inbox + userSeq 做增量同步

**状态**：Accepted

**背景**：用户可能离线、切后台、杀进程或切换设备。单纯在线推送无法保证所有消息可靠到达。

**决策**：服务端为每个用户维护 `inbox_events`，事件按 `userSeq` 递增。客户端保存 `sync_state.lastUserSeq`，收到 `NEW_EVENT_NOTIFY` 或登录后调用 `SYNC(lastUserSeq)` 拉取增量事件。

**后果**：

- WebSocket 只负责通知“有新事件”，可靠性由 Inbox 增量同步保证。
- 客户端先落库再推进 `lastUserSeq`，中断后可重复拉取，不丢消息。
- 需要维护事件去重和同步游标，服务端也要保证 `userSeq` 单调递增。

## ADR-004 使用 clientSeq / conversationSeq / userSeq 三套序列号

**状态**：Accepted

**背景**：IM 消息排序不能只依赖时间戳。失败重发、并发消息、群聊、多设备同步和分页都需要不同维度的顺序。

**决策**：

- `clientSeq`：客户端本地发送顺序。
- `conversationSeq`：服务端分配的会话内顺序。
- `userSeq`：服务端分配的用户收件箱顺序。

**后果**：

- 发送失败消息也能在本地按正确位置展示。
- 历史分页和搜索定位使用 `conversationSeq`，避免同时间戳断层。
- 断网恢复和增量同步使用 `userSeq`，避免漏事件。

## ADR-005 客户端以 SQLite 为页面数据源

**状态**：Accepted

**背景**：如果 UI 直接依赖网络回调，离线、重连、页面切换和批量同步时容易出现状态不一致。

**决策**：客户端 UI 主要读取本地 SQLite。网络事件进入后先由 `EventProcessor` 串行写库，再由 ViewModel 重新查询或监听状态变化刷新页面。

**后果**：

- 离线时仍可查看本地消息、头像和图片缓存。
- 页面展示和同步过程解耦。
- 需要设计本地表、迁移和多账号隔离，数据库复杂度上升。

## ADR-006 服务端当前采用 DataStore + MySQL 快照/镜像表

**状态**：Accepted

**背景**：项目早期以验证 IM 链路为主，直接搭建完整关系型写入模型成本较高，但又需要服务重启后数据不丢，并能通过数据库工具排查。

**决策**：以内存 `DataStore` 作为运行时权威状态；MySQL 中保存 `lightchat_state` 快照，同时刷新用户、会话、消息、Inbox 等镜像表。

**后果**：

- 运行时逻辑简单，服务重启可快速恢复。
- MySQL 镜像表便于观察和测试数据注入。
- 后续生产化应逐步改为各业务表事务写入，弱化或移除整体快照。

## ADR-007 图片使用 OSS 对象存储，消息只保存元信息

**状态**：Accepted

**背景**：图片原图体积较大，直接通过 WebSocket 传输会阻塞消息通道，也不利于缓存和 CDN。

**决策**：图片上传通过 HTTP multipart；服务端上传原图和缩略图到阿里云 OSS；消息 `extra` 保存 objectKey、原图 URL、缩略图 URL、尺寸、必要的降级信息。

**后果**：

- 消息链路保持轻量。
- 接收端可以先显示缩略图，用户点开再拉取原图。
- 私有 Bucket 的签名 URL 会过期，必须支持刷新并持久化新 URL。

## ADR-008 本地 Mock Push 替代厂商推送用于开发验证

**状态**：Accepted

**背景**：真实离线推送需要接入 FCM 或各厂商 SDK，并完成包名、签名、控制台和证书配置。当前阶段只要求三台本地设备验证“WebSocket 断开后能收到通知”。

**决策**：服务端在目标用户 WebSocket 不在线时，根据用户 ID 映射到 ADB 设备，通过显式广播模拟厂商推送。客户端 `BroadcastReceiver` 收到后展示本地通知。

**后果**：

- 能覆盖本地开发和演示场景。
- 不需要厂商 SDK 和外部推送账号。
- 不能代表生产级离线推送，拔掉 USB 或离开本地环境后不成立。

## ADR-009 Bootstrap 当前采用全量同步

**状态**：Accepted

**背景**：曾为了加快登录将 Bootstrap 限制为最近 200 条消息，但性能验证阶段需要新账号首次登录后完整同步 200 个测试好友、200 个会话和 1 万条测试消息。

**决策**：当前 Bootstrap 返回全量可见数据，并在日志中明确“全量数据同步完成”。

**后果**：

- 新账号本地数据库能完整恢复服务端状态。
- 首次登录大数据量账号会更慢。
- 后续生产化建议拆分为：基础资料 Bootstrap + 消息历史分页接口。

## ADR-010 长聊天记录使用双向分页和锚点恢复

**状态**：Accepted

**背景**：1 万条消息场景下，一次性加载会造成卡顿；从搜索结果进入中间消息后，还需要继续向上/向下加载。

**决策**：聊天页每页加载约 80 条，使用 `conversationSeq` 做上下分页 cursor；加载前记录可见锚点，加载后恢复滚动位置；搜索定位时把目标消息放在屏幕中部并高亮 3 秒。

**后果**：

- 大会话进入和滚动更稳定。
- 搜索定位不会强制回到底部。
- 实现复杂度增加，需要区分普通进入、搜索进入、子页面返回和输入栏聚焦等场景。
