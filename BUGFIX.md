# BUGFIX

本文记录 LightChat 开发中真实遇到的关键问题、排查过程、根因和修复结果。

## 1. 图片缩略图偶发生成失败，缩略图变成原图大小

**现象**：部分图片发送后缩略图大小接近原图，压缩没有生效；某些设备上 `BitmapFactory.decodeFile()` 返回 null。

**排查过程**：

1. 检查图片发送链路，发现上传成功但缩略图 fallback 到原图。
2. 继续追踪 `generateThumbnail()`，发现上游 `decodeSampledBitmap()` 返回 null。
3. 对比不同图片尺寸，发现当 `largest / maxDim` 计算出 3、5、6 这类值时更容易复现。

**根因**：`BitmapFactory.Options.inSampleSize` 必须是 2 的幂次方。旧逻辑用整数除法直接得到 `sampleSize = largest / maxDim`，例如 `1280 / 200 = 6`，部分设备对非法值处理不稳定，导致解码失败。

**修复**：

- 新增统一采样计算逻辑，只返回 1、2、4、8、16 等合法值。
- `ImageUtils.decodeSampledBitmap()`、`ChatViewModel.compressImage()`、相册缩略图和预览缩略图统一使用该逻辑。

**验证**：

- `rg inSampleSize` 检查所有使用点。
- 运行 `.\gradlew.bat testDebugUnitTest` 和 `.\gradlew.bat assembleDebug` 通过。
- 安装到三台设备后继续验证图片发送和缩略图展示。

## 2. 切换新账号后置顶和免打扰状态丢失

**现象**：新账号首次登录后，服务端本来有置顶和免打扰设置，但客户端本地同步后全部没有置顶状态。

**排查过程**：

1. 查看服务端日志，发现客户端可能带着旧账号的 `lastUserSeq` 请求增量同步。
2. 检查客户端本地会话数据，发现新账号本地还没有完整数据，但同步游标已经不是 0。
3. 检查登录保存流程，发现账号切换与 DAO 写入之间存在时序问题。

**根因**：登录新账号时，本地 session 和 owner 切换不够严格；`SharedPreferences.apply()` 异步提交可能导致 DAO 写入仍使用旧 owner，`needsBootstrap()` 判断也过于依赖 `lastUserSeq`。

**修复**：

- 登录保存时先切换当前用户，再执行本地数据写入。
- session 保存使用同步提交，避免 owner 竞态。
- `needsBootstrap()` 增加本地会话数量判断：`lastUserSeq <= 0` 或本地无可见会话时执行 Bootstrap。
- Sync 请求在本地无会话时强制从 `lastUserSeq = 0` 开始。

**验证**：

- 注入 200 位测试好友和 200 个会话后，新账号登录可完整同步会话。
- 置顶、免打扰、联系人数据能落入本地数据库并在 UI 展示。

## 3. 一万条聊天记录搜索定位后触顶、断层和自动上滑

**现象**：测试好友会话中有 1 万条消息时，搜索定位到中间消息后，有时高亮看不到；继续滚动时会自动向上加载历史，甚至屏幕快速上滑；向上能加载，向下加载不稳定。

**排查过程**：

1. 检查测试数据，发现每 100 条消息共用一个时间戳。
2. 查看分页 SQL，旧逻辑使用 `create_time` 作为历史分页 cursor。
3. 对比分页结果，发现同时间戳消息会被跳过或重复，导致列表锚点失效。

**根因**：时间戳不是会话内唯一递增值，不能作为长聊天记录分页边界。加载历史消息后列表项数量变化，若没有稳定锚点恢复，也会造成视觉上的自动滚动。

**修复**：

- 上翻分页 cursor 从 `createTime` 改为 `conversationSeq`。
- 每页加载数量调整为 80 条，搜索定位加载目标前后窗口。
- 加载前记录可见第一条消息 ID 和偏移，加载后恢复锚点。
- 搜索命中消息进入聊天页时居中展示并高亮 3 秒。

**验证**：

- 1 万条消息会话可定位到中间消息。
- 定位后可继续向上加载历史和向下加载新消息。
- 图片路径不再参与聊天记录搜索。

## 4. 真机相册“所有图片”显示暂无图片，但相册列表有封面

**现象**：真机打开相册选择页时，“所有图片”网格为空；点击相册下拉列表时，各相册能显示封面和数量。

**排查过程**：

1. 相册列表可显示封面，说明权限和 MediaStore 基础访问正常。
2. 对比 `loadAlbums()` 和 `loadPhotos()`，发现相册列表走普通查询，网格走分页查询。
3. 在真机上分页查询触发兼容性异常，catch 后直接返回空列表。

**根因**：部分真机/系统对 MediaStore Bundle 分页参数支持不一致；网格查询失败后没有 fallback，UI 只能显示“暂无图片”。

**修复**：

- 网格分页统一走兼容路径，必要时使用手动分页扫描。
- `loadPhotoPage(reset)` 使用 `try/finally` 保证失败、空页或异常后结束 loading。
- 如果下一页没有新增图片，立即设置 `hasMore=false`，避免底部永久加载占位。

**验证**：

- 真机 `content://media/external/images/media` 能查询到图片 ID。
- 相册网格和相册列表使用同一基础读取路径，避免列表有封面而网格为空。

## 5. 转发图片一直转圈，合并转发图片显示“图片已过期”

**现象**：逐条转发图片时，图片气泡偶尔一直显示加载占位且无法长按删除；合并转发详情页中的图片偶尔显示 `[图片已过期]`。

**排查过程**：

1. 查看转发后的 message，发现 `content` 被改成 `[图片]`。
2. 继续检查 `extra`，旧合并转发只保存 `sender/content/time`，没有保存 `messageType/extra`。
3. 当远端签名 URL 过期且本地无缓存时，详情页没有足够信息恢复图片。

**根因**：转发图片快照信息丢失；逐条转发和合并转发没有保存完整图片元信息和本地/缓存回退路径。

**修复**：

- 转发图片时优先查找本地原图、原图缓存和缩略图缓存。
- 逐条转发图片的 `content` 保留可用本地图片路径。
- 合并转发保存完整 `ForwardSnapshot`：`senderId`、`sender`、`messageType`、`content`、`displayContent`、`time`、`extra`。
- 远程下载失败后显示可点击、可长按的失败占位，而不是无限转圈。

**验证**：

- 新转发产生的图片带有 base64/缓存兜底。
- 图片过期占位可长按删除或转发，不会卡死在不可操作状态。

## 6. 头像多处只显示名字首字母，断网后头像消失

**现象**：转发列表、名片推荐、搜索页面、群成员列表、好友申请页面等位置没有拉取真实头像，只显示名字首字母；断网重启后原本缓存过的头像也消失。

**排查过程**：

1. 抓包和本地数据库确认服务端已返回头像 URL 和 `avatarVersion`。
2. 检查 UI，发现多个页面没有调用统一头像加载器。
3. 检查 App 启动逻辑，发现曾调用 `cleanStaleAvatars(context, emptyMap())`，传入空版本 Map 会清理全部缓存。

**根因**：

- 头像加载逻辑分散，部分页面没有使用 `AvatarCacheLoader`。
- 缓存清理接口被错误调用，断网时没有远端兜底。

**修复**：

- `LightChatAvatar` 增加 `userId` 参数，避免缓存冲突。
- 转发、名片、搜索、群成员、会话列表、好友申请、合并转发详情等页面统一走头像加载器。
- 移除启动时空 Map 清理，缓存有效性改由版本号比对控制。
- 离线时跳过网络下载，优先使用本地缓存回退。

**验证**：

- 各头像页面显示真实头像。
- 断网后已缓存头像仍可显示。
- 名片头像首帧优先读取内存/磁盘缓存，减少闪烁。

## 7. 双方都在聊天详情页时已读状态不能即时更新

**现象**：发送端发送消息后，接收端已经在当前聊天详情页并看到消息，但发送端需要接收端退出再进入聊天页，才显示已读。

**排查过程**：

1. 确认消息本身已通过 WebSocket 和 SYNC 到达接收端。
2. 检查 MARK_READ 触发条件，发现进入页面时会标记已读，但当前页收到新消息后没有及时补发当前最新 `conversationSeq`。
3. 检查发送端更新，发现 READ_NOTIFY 处理后没有及时刷新本地已读展示。

**根因**：已读触发点只覆盖“进入聊天页”，没有覆盖“当前聊天页收到新消息”；发送端收到对方已读后，本地状态刷新不够即时。

**修复**：

- 聊天详情页收到新消息并处于当前会话时，立即发送 `MARK_READ(latestConversationSeq)`。
- 服务端更新 `conversation_members.last_read_seq` 并向发送方发送 `READ_NOTIFY`。
- 发送端处理 READ_NOTIFY 后更新本地会话成员已读 seq，刷新最后一条本人消息下的已读标签。

**验证**：

- 两端都停留聊天详情页时，接收端看到消息后发送端可即时显示已读。

## 8. WebSocket 已连接但用户显示 null

**现象**：服务端日志中某个用户连接显示为 `null`，客户端表现为无法正常连接或收不到消息。

**排查过程**：

1. 抓取服务端日志，确认 TCP/WebSocket 连接已经建立。
2. 检查 AUTH 包发送时机，发现 `onOpen` 和 `webSocket` 赋值之间存在竞态。
3. 同时检查本地 token，发现过期 token 会导致服务端拒绝认证。

**根因**：WebSocket 连接建立不等于业务鉴权成功。AUTH 包可能因竞态丢失，或 token 过期导致连接没有注册到 `ConnectionRegistry`。

**修复**：

- 修复 `onOpen` 与 WebSocket 实例赋值时序，确保 AUTH 包不会丢。
- 过期 token 走重新登录流程。
- 连接注册只在 AUTH 成功后进行。

**验证**：

- 服务端连接表可看到真实 userId。
- 在线用户能收到 `NEW_EVENT_NOTIFY`，离线用户进入 Mock Push 路由。
